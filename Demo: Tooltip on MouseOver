# -*- coding: utf-8 -*-
"""
A demonstration of how to create Tooltips that appear on mouseover of a button
"""

from tkinter import *

# Define a function to show the tooltip when the mouse  the button.
def show_tooltip(event):
    tooltip_label.place(x=root.winfo_pointerx(), y=root.winfo_pointery())

# Define a function to hide the tooltip when the mouse leaves the button.
def hide_tooltip(event):
    tooltip_label.place_forget()

# Create the main Tkinter window.
root = Tk()

# Set the initial size of the window.
root.geometry("200x200")

# Set the title of the window.
root.title("Demo of Tooltip on MouseOver")

# Create a button widget with the text "Hover Me" and place it in the window.
button = Button(root, text="Hover Over Me")
button.pack(pady=20)

# Create a Label widget to display the tooltip.
tooltip_label = Label(root, text="This is a tooltip!", background="lightyellow", relief="solid", borderwidth=1)

# Initially hide the tooltip.
tooltip_label.place_forget()

# Bind the "<Enter>" event (mouse entering the button) to the show_tooltip function.
button.bind("<Enter>", show_tooltip)

# Bind the "<Leave>" event (mouse leaving the button) to the hide_tooltip function.

button.bind("<Leave>", hide_tooltip)

# Start the main event loop, which keeps the GUI application running.
root.mainloop()

